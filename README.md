# CrashReporter releases

Release host for the **RIS Crash Reporter** auto-updater. Each release ships the
Windows ('RIS-CrashReporter-Setup.exe') and MAC installer (`RIS-CrashReporter-Setup`); the reporter checks the latest
release here at startup and self-installs newer builds.

Source is kept private; only release binaries live here.
